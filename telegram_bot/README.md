# Telegram Bot - Docker Setup

Полнофункциональный Telegram бот для управления аккаунтами с админ панелью, встроенной базой данных SQLite и системой логирования.

## 📋 Требования

- Docker
- Docker Compose

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd telegram_bot
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл и добавьте ваши данные:

```env
BOT_TOKEN=your_telegram_bot_token_here
ADMIN_IDS=123456789,987654321
```

**Где найти:**
- `BOT_TOKEN`: Получите у [@BotFather](https://t.me/botfather) в Telegram
- `ADMIN_IDS`: Список ID администраторов разделенных запятой (без пробелов)

### 3. Запуск бота

```bash
docker-compose up -d
```

### 4. Проверка логов

```bash
docker-compose logs -f bot
```

### 5. Остановка бота

```bash
docker-compose down
```

## 📁 Структура проекта

```
telegram_bot/
├── app/
│   ├── handlers/          # Обработчики команд и событий
│   │   ├── admin.py       # Админ-панель
│   │   └── user.py        # Команды пользователя
│   ├── models.py          # Database модели (User, Account)
│   ├── utils/
│   │   ├── db_utils.py    # Работа с БД
│   │   ├── keyboards.py   # Inline кнопки
│   │   └── helpers.py     # Вспомогательные функции
│   └── database.db        # SQLite база данных
├── data/
│   └── uploads/           # Загруженные файлы пользователей
├── main.py                # Точка входа
├── config.py              # Конфигурация
├── requirements.txt       # Python зависимости
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose конфигурация
└── .env                   # Переменные окружения
```

## 🎯 Основные функции

### Для пользователей
- 📤 Загрузка архивов с аккаунтами
- 🌐 Запрос прокси
- 📱 Запрос номеров телефонов
- 💳 Прикрепление TRX-кошелька
- 🕒 Управление сменами (открытие/закрытие)

### Для администраторов
- 📋 Просмотр всех аккаунтов
- 👥 Управление пользователями (разрешение/запрещение доступа)
- 📊 Фильтрация аккаунтов по пользователю
- ⏳ Просмотр неотправленных аккаунтов
- 📢 Отправка уведомлений пользователям
- ➕ Добавление новых администраторов
- 🔒 Блокировка/разблокировка аккаунтов

## 🔧 Команды Docker

### Основные команды

```bash
# Запуск контейнера в фоне
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot

# Остановка контейнера
docker-compose down

# Перестроение образа
docker-compose up -d --build

# Просмотр статуса контейнера
docker-compose ps
```

### Управление БД

```bash
# Вход в контейнер
docker-compose exec bot bash

# Просмотр логов БД
docker-compose logs bot
```

## 📝 Логирование

Все действия пользователей и администраторов логируются в базу данных:
- Попытки входа
- Загрузка файлов
- Изменение статуса аккаунтов
- Управление доступом пользователей
- Отправка уведомлений

## 🔐 Безопасность

- Все файлы хранятся в отдельной директории `/app/data/uploads`
- Доступ ограничен только авторизованным пользователям
- Список администраторов контролируется через переменную окружения `ADMIN_IDS`
- База данных SQLite с persistent storage в Docker volume

## 💾 Сохранение данных при обновлении

**ВАЖНО:** База данных и загруженные файлы хранятся в Docker volume `bot_data`, который **НЕ будет удален** при обновлении бота.

### Процесс обновления:

```bash
# 1. Остановите текущий бот
docker-compose down

# 2. Обновите код (git pull или скопируйте новые файлы)
git pull

# 3. Перестройте образ и запустите
docker-compose up -d --build

# Данные БД и файлы загрузок полностью сохраняются!
```

### Проверка volumes:

```bash
# Просмотр всех volumes
docker volume ls

# Информация о volume
docker volume inspect telegram_bot_bot_data

# Локальное расположение данных
docker volume inspect --format='{{.Mountpoint}}' telegram_bot_bot_data
```

### Резервная копия БД:

```bash
# Создать backup
docker run --rm -v telegram_bot_bot_data:/data -v $(pwd):/backup alpine \
  sh -c 'cp /data/database.db /backup/database.db.backup'

# Восстановить из backup
docker run --rm -v telegram_bot_bot_data:/data -v $(pwd):/backup alpine \
  sh -c 'cp /backup/database.db.backup /data/database.db'
```

## 📦 Зависимости

Основные используемые библиотеки:
- `aiogram==3.22.0` - Telegram Bot API фреймворк
- `sqlalchemy==2.0.23` - ORM для работы с БД
- `aiosqlite==0.19.0` - Async SQLite драйвер
- `python-dotenv==1.0.0` - Загрузка переменных окружения

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверьте логи
docker-compose logs bot

# Убедитесь что .env файл создан правильно
cat .env

# Перестроите образ
docker-compose up -d --build
```

### Проблемы с загрузкой файлов

```bash
# Проверьте права доступа к директории uploads
docker-compose exec bot ls -la /app/data/uploads

# Создайте директорию если её нет
docker-compose exec bot mkdir -p /app/data/uploads
```

### Бот получает старые сообщения

Это нормально при первом запуске. Бот обрабатывает очередь сообщений из Telegram. Просто дождитесь синхронизации.

### БД потеряна после обновления

Если БД потеряна, это значит что был использован `docker-compose down -v`. Проверьте что в docker-compose.yml используется volume:

```yaml
volumes:
  - bot_data:/app/data  # ✅ Правильно - volume
  # НЕ используйте: ./data:/app/data  # ❌ Это bind mount
```


## 📞 Поддержка

Для проблем и вопросов создавайте Issue в репозитории.

## 📄 Лицензия

MIT License - см. LICENSE файл для деталей
