# Telegram Bot Account Management System

## 📋 Описание

Телеграм-бот для управления аккаунтами и уведомлениями пользователей. Бот позволяет:
- **Пользователям** отправлять архивы аккаунтов, запрашивать прокси и номера, прикреплять TRX-кошельки
- **Администраторам** управлять пользователями, просматривать архивы, отправлять уведомления

## 🚀 Быстрый старт

### Требования
- Python 3.9+
- pip

### Установка

1. **Клонируйте репозиторий и перейдите в директорию:**
```bash
cd telegram_bot
```

2. **Создайте виртуальное окружение (рекомендуется):**
```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env` на основе `.env.example`:**
```bash
cp .env.example .env
```

5. **Отредактируйте файл `.env` и добавьте:**
```
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
ADMIN_IDS=123456789,987654321
```

Где:
- `BOT_TOKEN` - токен вашего бота из BotFather
- `ADMIN_IDS` - Telegram ID администраторов (через запятую)

### Запуск бота

```bash
python main.py
```

## 👤 Функции пользователя

### Главное меню
При отправке `/start` пользователь видит следующие кнопки:
- **📤 Отправить аккаунт** - загрузить архив аккаунта
- **🌐 Запросить прокси** - отправить запрос администратору на прокси
- **📱 Запросить номера** - отправить запрос администратору на номера DaisySMS
- **💳 Прикрепить TRX-кошелек** - сохранить адрес TRX-кошелька

### Отправка архива
1. Нажимаем "📤 Отправить аккаунт"
2. Загружаем архив (ZIP, RAR, 7Z, TAR.GZ)
3. Архив сохраняется в БД с метаданными:
   - Идентификатор файла
   - Telegram ID отправителя
   - Username отправителя
   - Дата и время загрузки
   - Месяц (YYYY-MM)
4. Администратор получает уведомление

### Управление доступом
- При первом обращении `/start` пользователю запрещен доступ
- Администратор получает уведомление о новом пользователе
- Администратор может разрешить или запретить доступ

## 👨‍💼 Функции администратора

### Активация панели админа
```bash
/admin
```

### Главное меню администратора
- **📋 Просмотр аккаунтов** - просмотр всех архивов, по месяцам, неотправленных
- **👥 Управление пользователями** - управление доступами пользователей
- **📢 Отправить уведомление** - отправить уведомление одному или всем пользователям
- **🔐 Управление доступами** - разрешить/запретить доступ пользователям

### Команды администратора

#### Просмотр и управление пользователями
```bash
/list_users              # Список всех пользователей
/user_info [ID]          # Информация о пользователе
/allow_user [ID]         # Разрешить доступ пользователю
/deny_user [ID]          # Запретить доступ пользователю
```

#### Примеры:
```bash
/allow_user 123456789    # Разрешить доступ пользователю с ID 123456789
/deny_user 987654321     # Запретить доступ пользователю с ID 987654321
/user_info 123456789     # Получить информацию о пользователе
```

### Просмотр аккаунтов
- **📅 По месяцам** - выбрать месяц в формате YYYY-MM (например 2024-11)
- **📊 Все аккаунты** - показать все загруженные архивы
- **⏳ Неотправленные** - показать архивы со статусом "Не отправлен"

Для каждого аккаунта можно:
- Изменить статус отправки (✅ Отправлено / ❌ Не отправлено)
- Изменить статус блокировки (🔒 Заблокирован / 🔓 Разблокирован)

### Отправка уведомлений

Типы уведомлений:
- **💰 Зарплата выдана** - уведомление о выплате зарплаты
- **📞 Назначен созвон** - уведомление о предстоящем созвоне
- **⚠️ Назначен штраф** - уведомление о штрафе

Получатели:
- **👤 Конкретному пользователю** - введите Telegram ID пользователя
- **👥 Всем пользователям** - отправить всем пользователям с разрешенным доступом

## 📁 Структура проекта

```
telegram_bot/
├── main.py                 # Точка входа приложения
├── config.py               # Конфигурация и переменные окружения
├── requirements.txt        # Зависимости проекта
├── .env.example           # Пример файла конфигурации
├── app/
│   ├── __init__.py
│   ├── models.py          # Модели БД (User, Account, Log)
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── user.py        # Обработчики для пользователей
│   │   └── admin.py       # Обработчики для администраторов
│   └── utils/
│       ├── __init__.py
│       ├── db_utils.py    # Репозитории для работы с БД
│       ├── keyboards.py   # Клавиатуры и кнопки
│       └── helpers.py     # Вспомогательные функции
└── data/
    ├── uploads/          # Загруженные архивы пользователей
    └── bot.db            # База данных SQLite
```

## 💾 База данных

### Таблица `users`
```sql
id              INTEGER PRIMARY KEY
tg_id           INTEGER UNIQUE NOT NULL
username        VARCHAR(255)
trx_wallet      VARCHAR(255)
access          BOOLEAN DEFAULT FALSE
created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
```

### Таблица `accounts`
```sql
id              INTEGER PRIMARY KEY
user_id         INTEGER FOREIGN KEY -> users.id
month           VARCHAR(7) - формат YYYY-MM
file_path       VARCHAR(512)
sent            BOOLEAN DEFAULT FALSE
locked          BOOLEAN DEFAULT FALSE
date_created    DATETIME DEFAULT CURRENT_TIMESTAMP
```

### Таблица `logs`
```sql
id              INTEGER PRIMARY KEY
action_type     VARCHAR(50)
user_id         INTEGER FOREIGN KEY -> users.id
admin_id        INTEGER FOREIGN KEY -> users.id
description     TEXT
timestamp       DATETIME DEFAULT CURRENT_TIMESTAMP
```

## 🔒 Управление доступом

1. **При первом обращении** пользователь создается с `access=False`
2. **Администратор** получает уведомление о новом пользователе
3. **Администратор** может:
   - Разрешить доступ командой `/allow_user [ID]`
   - Запретить доступ командой `/deny_user [ID]`
4. **После разрешения** пользователь может использовать все функции бота

## 📝 Логирование

Логи сохраняются в файл `bot.log` и выводятся в консоль. Логируются:
- Попытки входа
- Загрузки архивов
- Запросы прокси и номеров
- Прикрепление кошельков
- Изменение прав доступа
- Отправка уведомлений

## ⚙️ Дополнительная конфигурация

### Время хранения архивов
В файле `config.py` можно изменить:
```python
ARCHIVE_RETENTION_DAYS = 30  # Хранить архивы 30 дней
```

### Несколько администраторов
В файле `.env` можно указать несколько администраторов:
```
ADMIN_IDS=123456789,987654321,555666777
```

## 🐛 Решение проблем

### Ошибка "BOT_TOKEN не установлен"
- Проверьте файл `.env`
- Убедитесь, что переменная `BOT_TOKEN` установлена

### Ошибка "Доступ запрещен" при запуске /admin
- Проверьте свой Telegram ID
- Убедитесь, что ваш ID добавлен в `ADMIN_IDS` в файле `.env`

### Проблемы с загрузкой файлов
- Проверьте права доступа к папке `data/uploads/`
- Убедитесь, что достаточно места на диске

## 📞 Контакты

Для вопросов и предложений обратитесь к разработчикам.

## 📄 Лицензия

MIT License

## 🔄 Обновление и поддержка

Для обновления зависимостей выполните:
```bash
pip install -r requirements.txt --upgrade
```
