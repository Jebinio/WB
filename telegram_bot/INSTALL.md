# Telegram Bot - Инструкция по запуску

## Windows 10/11

### Первый запуск (Полная установка)

1. **Скачайте Python 3.9+ с [python.org](https://www.python.org/downloads/)**
   - При установке обязательно отметьте "Add Python to PATH"

2. **Откройте PowerShell в папке проекта:**
   - Нажмите Shift + Right Click в папке проекта
   - Выберите "Open PowerShell window here"

3. **Запустите скрипт установки:**
   ```powershell
   .\setup.bat
   ```
   Или вручную:
   ```powershell
   python -m venv venv
   .\venv\Scripts\activate
   pip install -r requirements.txt
   copy .env.example .env
   ```

4. **Отредактируйте файл `.env`:**
   - Откройте `telegram_bot\.env` в текстовом редакторе
   - Замените `YOUR_BOT_TOKEN_HERE` на ваш токен от BotFather
   - Добавьте ваш Telegram ID в `ADMIN_IDS`
   
   Пример:
   ```
   BOT_TOKEN=1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefg
   ADMIN_IDS=123456789
   ```

5. **Запустите бот:**
   ```powershell
   python main.py
   ```

### Последующие запуски

1. **Откройте PowerShell в папке проекта**

2. **Активируйте виртуальное окружение:**
   ```powershell
   .\venv\Scripts\activate
   ```

3. **Запустите бот:**
   ```powershell
   python main.py
   ```

## Linux / macOS

### Первый запуск

1. **Откройте терминал в папке проекта**

2. **Запустите скрипт установки:**
   ```bash
   chmod +x setup.sh
   ./setup.sh
   ```
   Или вручную:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   cp .env.example .env
   ```

3. **Отредактируйте файл `.env`:**
   ```bash
   nano .env
   # или используйте ваш любимый редактор
   ```

4. **Запустите бот:**
   ```bash
   python main.py
   ```

### Последующие запуски

```bash
source venv/bin/activate
python main.py
```

## 🔑 Получение токена BotFather

1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/botfather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен в формате: `1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefg`

## 🆔 Получение вашего Telegram ID

1. Откройте Telegram
2. Найдите бота [@userinfobot](https://t.me/userinfobot)
3. Отправьте любое сообщение
4. Бот вернет ваш ID

## 🚀 Первые шаги после запуска

1. **Убедитесь, что бот запущен:**
   ```
   2024-11-14 10:30:45 - root - INFO - Бот запущен и готов к работе
   ```

2. **Отправьте боту команду `/start`**

3. **Администратор должен разрешить доступ:**
   ```
   /allow_user 123456789
   ```
   (Где 123456789 - это ваш ID)

4. **Теперь вы можете использовать все функции!**

## 🔴 Остановка бота

- **Windows/Linux/macOS:** Нажмите `Ctrl + C` в окне терминала

## ⚠️ Решение проблем

### "Module not found" ошибка
```powershell
# Убедитесь что виртуальное окружение активировано
.\venv\Scripts\activate

# Установите зависимости еще раз
pip install -r requirements.txt
```

### "BOT_TOKEN не установлен"
- Проверьте что файл `.env` существует
- Проверьте что переменная `BOT_TOKEN` в файле заполнена
- Перезапустите бот

### Бот не отвечает
- Проверьте интернет соединение
- Проверьте логи в `bot.log`
- Убедитесь что токен верный
- Перезапустите бот

### "Access Denied" при запуске скрипта
**Windows:**
```powershell
# Дайте разрешение на запуск скриптов
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## 📊 Проверка работы

После запуска бота вы должны увидеть:
```
================================================== ==
Запуск Telegram бота...
================================================== ==
✅ База данных инициализирована
✅ Бот инициализирован
👨‍💼 Администраторы: [123456789]
================================================== ==
🚀 Бот запущен и готов к работе
================================================== ==
```

## 💾 Резервная копия БД

Периодически создавайте резервные копии папки `data/`:
```powershell
# Windows
Copy-Item -Path "data" -Destination "data_backup_$(Get-Date -Format 'yyyy-MM-dd')" -Recurse
```

```bash
# Linux/macOS
cp -r data data_backup_$(date +%Y-%m-%d)
```

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте файлы `bot.log` и `README.md`
2. Посмотрите примеры в `EXAMPLES.md`
3. Проверьте вывод консоли на ошибки
